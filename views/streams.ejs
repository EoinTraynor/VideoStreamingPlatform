<div class="search">
	<div class="container">
		<h2>Search Results</h2>
		<table class="table table-hover">
			<tbody>
					<tr ng-repeat="stream in streams">
						<td>{{ stream.name }}</td>
					</tr>
			</tbody>
		</table>
	</div>
</div>
